type: object
properties:
  code:
    type: string
    description: device_code from the initial method.
  client_id:
    type: string
    description: Get this from your app settings.
  client_secret:
    type: string
    description: Get this from your app settings.
required:
  - code
  - client_id
  - client_secret